# TrackApp — instrucciones para Claude

## Git
- Trabajar siempre en la rama `main`. No crear ramas nuevas.
- Hacer commit y push directo a `main` al terminar cada cambio.
- Usar `git push -u origin main`.
